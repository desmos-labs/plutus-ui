# don't ever lint node_modules
node_modules

# don't lint build output (make sure it's set to your correct build folder name)
build

# don't lint config files
craco.config.js
tailwind.config.js
postcss.config.js

# don't format the assets
src/assets
